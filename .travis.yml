language: objective-c
before_install:
- gem i cocoapods --no-ri --no-rdoc
- pod install --repo-update
script:
  - set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace SCITokenSearchField.xcworkspace -scheme SCITokenSearchField -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
  - pod lib lint
